https://github.com/aile-dev02/TestCaseGenReports
テスト担当者がMDファイルに記載し、リポジトリにpushすることでGitHub Actionsのワークフローが走りエクセルファイルの成果物を生成する仕組みを構築
エクセルに直接記入する必要はなく、Gitを通したMDファイルの変更でバージョンを管理する
生成されるレポートがダッシュボードの役割を持つ
上記リポジトリをテンプレートとして、各プロジェクトにそのまま追加する

LMM統合IDEでソースファイルを辿ってある程度のテストケースの複製などが出来るようにしたい(オリジナルは人力)
また、設計書の精度が高ければ、開発時にも設計書を解析してクオリティの高いコードの生成が見込める
そのためにも異常系などを含めたテスト観点の設計書が重要となる

1. Git管理するもの / しないものを分ける
2. 人が読むものはMarkdown、証跡はバイナリ
3. 「最新」と「履歴」を分離する
4. IDで紐づける（テストケースID / チケットID）

- テストデータ
保管場所、ファイル命名規則

- エビデンス
保管場所、ファイル命名規則、形式
画像を使用すると、以下のデメリットが挙げられるため、極力言語化することとする。ただし、UI関係など画像が必要となるテストもある。
・後から検索できない
・差分比較しづらい
・「何が問題なのか」を読む側が解釈する必要がある
・AIにも解析しづらい
・レビューコストが高い
Gitに入れると重くなるから別で管理するかgitignoreで除外
テストケースからリンクで飛べるようにする

- 動作確認書
差分を確認するため、ExcelではなくMDにする
● 内容
・確認観点
・環境
・結果（OK/NG）
・エビデンスリンク

- オンボーディング資料
● 内容
・環境構築
・テスト実行手順
・よくある失敗
・用語

- テスト設計時のAcceptance Criteria
Gherkinで記述
● ポイント
・テストケースの元になるので超重要
・曖昧表現NG（「適切に」など）

- リグレッションテストのためのテストケース
● 内容
・前提条件
・手順
・期待結果
・対応AC
● ポイント
・壊れたら困るものだけに絞る
・全部入れると膨大になるため、絞る

- QAダッシュボード
● ツール
・C-HUB
・Excel(自動生成)
● 内容
テスト進捗
バグ件数
品質トレンド
● ポイント
「今どうなってるか」が一目で分かる
エンジニアよりPM向け

- テスト完了報告書
● 内容
・範囲
・実施件数
・バグ件数
・未解決リスク
・Go/NoGo判断
● ポイント
・経営・PM向け
・細かいログは書かない（ダッシュボード参照）

- バグチケット
● 必須項目
・再現手順
・期待結果 / 実際結果
・環境
・エビデンスリンク
● ポイント
・「再現できるか」がすべて
・感想は書かない
1. バグ管理環境をToDoアプリ等やGithub Issuesで統一
2. バグの発生状況や修正の優先順位が判断できること(不具合の再現方法が一目でわかるような起票をする)
3. バグが長期間放置されないこと(バグチケットの有効期限を設定し、期日になったら優先度を上げるかクローズするか判断する)

- ディレクトリ構造例
├── master/
│   ├── testcases/            # テストケース Markdown (.md)
│   ├── requirements/         # 要件定義 Markdown (.md)
│   └── matrices/             # 手動管理の補助マトリクス (任意)
│
├── runs/
│   └── YYYY-MM-release/      # リリース単位の実行セット
│       ├── results.yml       # テスト結果
│       └── evidence/         # スクリーンショット等
│
├── reports/
│   ├── latest/               # 最新生成物 (Git管理外推奨)
│   └── archive/              # 過去レポートのアーカイブ
│
├── schemas/
│   ├── testcase.ts           # テストケース FrontMatter スキーマ
│   ├── results.ts            # 実行結果スキーマ
│   └── requirement.ts        # 要件 FrontMatter スキーマ
│
├── scripts/
│   ├── validate-testcases.ts # バリデーション CLI
│   ├── generate-excel.ts     # Excel レポート生成
│   ├── generate-summary.ts   # QA サマリ生成
│   ├── generate-traceability.ts # トレーサビリティ生成
│   └── lib/
│       ├── types.ts          # 共通型定義
│       ├── parser.ts         # Markdown パーサ (remark)
│       ├── loader.ts         # ファイルローダー
│       └── excel-builder.ts  # Excel ビルダー (exceljs)
│
├── .github/
│   └── workflows/
│       ├── qa-report.yml     # push 時レポート生成
│       └── qa-validate.yml   # PR/push 時バリデーション
│
├── package.json
├── tsconfig.json
└── README.md

## MDからエビデンスへのリンク
[]で見出しを作り、すぐ右に()でリンクを設定する

- [最新の結果を見る](../../evidence/manual/TC001_ログイン/latest/)

![ログイン成功](../../evidence/manual/TC001_ログイン/latest/screenshot1.png)
